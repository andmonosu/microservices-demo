spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/andmonosu/microservices-demo-config-server
          username: andmonosu
          clone-on-start: true
          password:
          default-label: main
    fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: springCloud_Pwd!
